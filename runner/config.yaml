agentos_root: /home/statpan/workspace/libs/agentproc
layers:
  subprocess: codex
  thread: gemini
runner:
  hidden_runtime: true
  max_concurrent: 0
  poll_interval: 5s
  mode: daemon
  thread_fallback_models:
    - gemini-3.1-pro-preview
